=begin
#EVE Stable Infrastructure (ESI) - tranquility

#No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)

The version of the OpenAPI document: 2020-01-01

Generated by: https://openapi-generator.tech
Generator version: 7.20.0-SNAPSHOT

=end

require 'date'
require 'time'

module ESI
  class CorporationsCorporationIdShareholdersGetInner < ApiModelBase
    attr_accessor :share_count

    attr_accessor :shareholder_id

    attr_accessor :shareholder_type

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'share_count' => :'share_count',
        :'shareholder_id' => :'shareholder_id',
        :'shareholder_type' => :'shareholder_type'
      }
    end

    # Returns attribute mapping this model knows about
    def self.acceptable_attribute_map
      attribute_map
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      acceptable_attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'share_count' => :'Integer',
        :'shareholder_id' => :'Integer',
        :'shareholder_type' => :'String'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `ESI::CorporationsCorporationIdShareholdersGetInner` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      acceptable_attribute_map = self.class.acceptable_attribute_map
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!acceptable_attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `ESI::CorporationsCorporationIdShareholdersGetInner`. Please check the name to make sure it's valid. List of attributes: " + acceptable_attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'share_count')
        self.share_count = attributes[:'share_count']
      else
        self.share_count = nil
      end

      if attributes.key?(:'shareholder_id')
        self.shareholder_id = attributes[:'shareholder_id']
      else
        self.shareholder_id = nil
      end

      if attributes.key?(:'shareholder_type')
        self.shareholder_type = attributes[:'shareholder_type']
      else
        self.shareholder_type = nil
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if @share_count.nil?
        invalid_properties.push('invalid value for "share_count", share_count cannot be nil.')
      end

      if @shareholder_id.nil?
        invalid_properties.push('invalid value for "shareholder_id", shareholder_id cannot be nil.')
      end

      if @shareholder_type.nil?
        invalid_properties.push('invalid value for "shareholder_type", shareholder_type cannot be nil.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if @share_count.nil?
      return false if @shareholder_id.nil?
      return false if @shareholder_type.nil?
      shareholder_type_validator = EnumAttributeValidator.new('String', ["character", "corporation"])
      return false unless shareholder_type_validator.valid?(@shareholder_type)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] share_count Value to be assigned
    def share_count=(share_count)
      if share_count.nil?
        fail ArgumentError, 'share_count cannot be nil'
      end

      @share_count = share_count
    end

    # Custom attribute writer method with validation
    # @param [Object] shareholder_id Value to be assigned
    def shareholder_id=(shareholder_id)
      if shareholder_id.nil?
        fail ArgumentError, 'shareholder_id cannot be nil'
      end

      @shareholder_id = shareholder_id
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] shareholder_type Object to be assigned
    def shareholder_type=(shareholder_type)
      validator = EnumAttributeValidator.new('String', ["character", "corporation"])
      unless validator.valid?(shareholder_type)
        fail ArgumentError, "invalid value for \"shareholder_type\", must be one of #{validator.allowable_values}."
      end
      @shareholder_type = shareholder_type
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          share_count == o.share_count &&
          shareholder_id == o.shareholder_id &&
          shareholder_type == o.shareholder_type
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [share_count, shareholder_id, shareholder_type].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

  end

end
